# Wingfoil Instructor Static Clone

Authorized static reproduction of `wingfoilinstructor.com` for GitHub Pages.

The generated pages keep the original layout, navigation, text, cookie layer,
slideshow markup, and externally hosted CM4all assets. Internal links are
rewritten to work from a GitHub Pages project URL.

To refresh the static copy:

```sh
python3 tools/mirror_site.py
```
